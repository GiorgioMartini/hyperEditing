# Pipeline Motion Graphics — Defaults & Design References

Every video generated by the pipeline uses these shared defaults. Override per project via `project.json`.

## Design references (read before changing templates)

| Doc | Purpose |
|-----|---------|
| [`MOTION_PHILOSOPHY.md`](../MOTION_PHILOSOPHY.md) | Aesthetic laws, grid/chrome recipes (§3.2–3.4) |
| [`.claude/skills/short-form-video/SKILL.md`](../.claude/skills/short-form-video/SKILL.md) | Layout, audio-sync protocol, verification gate |
| [`pipeline/BRAND.md`](BRAND.md) | Brand presets and per-video taste |
| [`DESIGN.ais-example.md`](../DESIGN.ais-example.md) | Brand token example (AIS social-navy preset) |

## Pipeline defaults

| Setting | Default | Override |
|---------|---------|----------|
| Layout | `short-form-split` | `project.json` → `layout.mode` |
| Brand preset | `dark-chrome` | `project.json` → `brand.preset` |
| Beat density | ~1 per 2.5s | `motion.beatIntervalSec` |
| Beat duration | 1.5–4s | `motion.minBeatDuration` / `maxBeatDuration` |
| Hero beat | every 5s | `motion.jawDropperEverySec` |
| MG layout | `top-half` | `motionGraphic.layout` in beat plan |
| Caption placement | bottom (split) / center (legacy) | automatic from layout |
| Caption gap | `0.15s` between lines | `captions.interSegmentGapSec` |
| Scene transitions | CSS push (split layout) | `motion.useRegistryTransitions: true` to disable |

## Layout modes

| Mode | Description |
|------|-------------|
| `short-form-split` | Top-half MG scenes, face in bottom half, ambient bg, seam, face modes |
| `upper-card` | Legacy: small MG card over full-bleed face |
| `backdrop-pip` | YouTube backdrop + PiP B-roll |

## Motion graphic templates (12 recipes)

Located in `pipeline/templates/mg/`:

| Template | Use case |
|----------|----------|
| `stat-slam` | Hero stat with motion streak |
| `stat-callout` | Big number + label |
| `kinetic-type` | Phrase with word stagger |
| `list-reveal` | Title + up to 3 bullets |
| `card-grid-3` | Three benefit cards |
| `chromatic-slam` | Word swap + glitch/chromatic ghost |
| `stamp-reject` | X stamp over chip grid |
| `contrast-flip` | Word A → Word B morph |
| `badge-list` | Numbered pills |
| `hero-quote` | Large quote + attribution |
| `path-draw-icon` | SVG path draw + label |
| `counter-up` | Animated count-up stat |

Shared CSS partials live in `templates/mg/_shared/` (grid, chrome type, glass panel, chromatic ghost, sparks, secondary motion).

Scaffold compositions: `templates/scaffold/` (ambient-bg, seam-treatment, scene-transition).

## Speech-bounded timing

Motion graphics appear only while the speaker is talking:

| Concept | Source | Behavior |
|---------|--------|----------|
| Speech window | `getSpeechWindow()` in `transcript-anchor.ts` | First spoken word start → last spoken word end |
| Beat budget | `computeBeatBudget(speechDuration, …)` | ~1 beat per 2.5s of **speech**, not full video length |
| Anchor phrase | Gemini → `anchorPhrase` field | Must match transcript verbatim; code resolves exact timestamp |
| Phrase match | `findPhraseMatch()` | exact → numeric (`5`/`five`) → prefix (min 2 words) |
| Overlap policy | `resolveBeatTimings()` | Trim previous beat; drop later beat if unresolvable |
| Composition end | Stage 6 compose | Duration = speech end + small hold — no MG in trailing silence |

## Verification checklist (every video)

After `npm run pipeline`:

1. `cd video-projects/<slug> && npx hyperframes lint`
2. Preview: `npx hyperframes preview` — scrub `?comp=captions`, `?comp=ambient-bg`, each `?comp=mg-beat-N`
3. Confirm:
   - Split layout: face in bottom half during top-half beats; fullscreen on hero beats
   - Ambient grid visible (not flat black/navy)
   - Seam feather at y=960 during bottom-half modes
   - No overlapping caption lines during fast speech
   - No MG or B-roll after the last spoken word (composition ends at speech + hold)
   - MG appears when anchor phrase is spoken (±0.2s)
   - Visual beat every ≤3.5s; hero beat every ~5s
4. Draft render + frame extraction at anchor timestamps
5. Re-run stages after code changes:
   - `--stage 4` — beat planning / anchor sync / quality replan
   - `--stage 5` — MG template changes
   - `--stage 6` — compose / scaffold / caption changes
